sequenceDiagram
    participant 成员 as 跑团成员
    participant UI as 登录界面
    participant Controller as 认证控制器
    participant Member as 成员实体

    成员->>UI: 点击“微信一键登录”
    UI->>Controller: wechatLogin()
    Controller->>Controller: 调用微信接口获取openId
    Controller->>Member: findMemberByOpenId(openId)
    Member-->>Controller: 返回成员信息（已绑定）
    Controller-->>UI: 返回登录成功
    UI-->>成员: 跳转至首页
